application.desc=A simple java app to manage passwords.
application.homepage=
application.title=Password Vault
application.vendor=Patrick Morgan
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/PassVault.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.passvault-xerces=src/passvault/xerces
file.reference.resolver.jar=../../../Desktop/xerces/resolver.jar
file.reference.resolver.jar-1=src/passvault/xerces/resolver.jar
file.reference.serializer.jar=../../../Desktop/xerces/serializer.jar
file.reference.xercesImpl.jar=../../../Desktop/xerces/xercesImpl.jar
file.reference.xml-apis.jar=../../../Desktop/xerces/xml-apis.jar
includes=**
jar.compress=true
javac.classpath=\
    ${libs.swing-app-framework.classpath}:\
    ${libs.swing-layout.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jnlp.codebase.type=local
jnlp.codebase.url=file:/Users/patrick/dev/java/PassVault/dist
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false
main.class=passvault.PassVaultApp
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Dcom.apple.mrj.application.apple.menu.about.name="Password Vault" -Dcom.apple.mrj.application.growbox.intrudes=false -Xdock:name="Password Vault" -Dapple.laf.useScreenMenuBar=true
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
